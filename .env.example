# ============================================================
# FINE-APIHUB v2 环境变量示例
# 用法：cp .env.example .env.local   （Next.js 会自动加载 .env.local；db/migrate.js 依次加载 .env、.env.local）
# 默认零配置即可运行：不设任何变量 = SQLite 单文件模式，库在 <项目根>/data/fine-apihub.db
# ============================================================

# ---------- 数据库：默认 SQLite（无需任何 DB_* ）----------
# 数据库文件位置（相对路径基于启动时的工作目录）。默认：<项目根>/data/fine-apihub.db
# DB_PATH=./data/fine-apihub.db

# ---------- 切换到外部 MySQL（可选）----------
# 只有设了 DB_DRIVER=mysql 时，下面的 DB_* 才会被读取（db/pool.js::getPool）
# DB_DRIVER=mysql
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_USER=relay
# DB_PASSWORD=
# DB_NAME=relay_monitor

# ---------- 时区 ----------
# 「今日消耗 / 用量统计」的日期边界按此时区计算（Docker 镜像内已装 tzdata）
# TZ=Asia/Shanghai
# 日报的统计日界线与定时发送时区；非法值会回退默认。默认：Asia/Shanghai
# REPORT_TIME_ZONE=Asia/Shanghai

# ---------- v1 数据一次性迁移（可选）----------
# 指向 v1 的 data 目录（须含 stations.json；另有 history.json / secret.key 会被一并导入）。
# 仅在「库为空」时导入，幂等、绝不覆盖已有数据；导入完成后即可删除此项。
# V1_DATA_DIR=./data

# ---------- Docker 构建期变量（非运行时）----------
# 由 Dockerfile 的 ARG GIT_SHA 注入，用于 /api/meta 显示 commit；本地开发无需设置。
# APP_COMMIT=
